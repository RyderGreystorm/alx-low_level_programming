#include "main.h"

/**
 *char *_strcpy - copy a string to another
 * @dest: destination string of the function
 * @src: string from source getting appended to other string
 *Return: char
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
